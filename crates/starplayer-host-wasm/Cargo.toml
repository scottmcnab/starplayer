[package]
name = "starplayer-host-wasm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

# `cdylib` is what the browser loads; `rlib` keeps the crate usable as a normal Rust
# dependency so `cargo test --workspace` can exercise the sine table, the command ring
# and the peak meter on the host without a browser in the loop.
[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
starplayer = { workspace = true, features = ["std"] }
wasm-bindgen = { workspace = true }
