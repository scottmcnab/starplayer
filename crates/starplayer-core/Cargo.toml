[package]
name = "starplayer-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[features]
default = []
# `std` is never part of any default feature set (AGENTS.md design goal 4).
std = []
alloc = []
serde = []
